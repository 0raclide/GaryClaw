# Design: Implement Skill Hardening — Already Complete (No-Op)

Generated by /office-hours on 2026-03-31
Branch: main
Repo: 0raclide/GaryClaw
Status: APPROVED
Mode: Builder
Review: 1 round, 3 issues found and fixed, quality 7→9/10
Supersedes: docs/designs/implement-skill-hardening.md

## Problem Statement

The prioritize skill selected "Implement Skill Hardening (Unresolved Review Findings)" as the highest-impact work. However, all three changes specified in the original design doc (`docs/designs/implement-skill-hardening.md`, Status: APPROVED) have already been implemented in `src/implement.ts` and `src/pipeline.ts`.

This is a no-op design doc. Its purpose is to signal to the implement skill that no work remains.

## Verification Evidence

| Finding | Status | Evidence |
|---------|--------|----------|
| Static import in pipeline.ts | DONE | `src/pipeline.ts` line 20: `import { buildImplementPrompt } from "./implement.js"` |
| `validateImplementationOrder()` | DONE | `src/implement.ts` lines 106-118, wired at line 271 in `buildImplementPrompt()` |
| `formatReviewContext` actionable filter | DONE | `src/implement.ts` lines 122-139 (`FormatReviewOptions`, `isActionableDecision`, `ACTION_KEYWORDS`), wired at line 286 with `{ actionableOnly: true }` |

All three changes match the original design doc spec exactly. Existing tests cover all changes:
- `test/implement.test.ts` (48 tests)
- `test/implement-loaddesigndoc.regression-1.test.ts` (7 tests)
- `test/pipeline-implement.test.ts` (4 tests)

## Root Cause

The TODOS.md entry for this item was not struck through after the work shipped. The prioritize skill's completed-item detection (`extractCompletedTitles`) only checks for `~~strikethrough~~` markers in TODOS.md headings. If the heading isn't struck through, the item appears actionable even though the code is already in main.

## Recommended Action

Strike through the "Implement Skill Hardening" entry in TODOS.md so it won't be re-picked. This is a manual markup edit, not a code change. The implement skill will separately recognize there is no work to do by checking the Implementation Order section below (empty or trivial steps means skip execution).

## Implementation Order

1. Strike through "Implement Skill Hardening" heading in TODOS.md (manual markup task, not source code)
2. No source code changes needed. All three original findings are already implemented and tested.

## Success Criteria

1. This design doc exists in `docs/designs/` so the implement skill discovers it
2. The implement skill recognizes there is no work to do (Implementation Order contains no source code steps, only a TODOS.md markup task)
